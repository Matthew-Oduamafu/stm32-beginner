/**
 ******************************************************************************
 * @file           : main.c
 * @author         : Auto-generated by STM32CubeIDE
 * @brief          : Main program body
 ******************************************************************************
 * @attention
 *
 * Copyright (c) 2025 STMicroelectronics.
 * All rights reserved.
 *
 * This software is licensed under terms that can be found in the LICENSE file
 * in the root directory of this software component.
 * If no LICENSE file comes with this software, it is provided AS-IS.
 *
 ******************************************************************************
 */

#include <stdint.h>

#if !defined(__SOFT_FP__) && defined(__ARM_FP)
  #warning "FPU is not initialized, but the project is compiling for an FPU. Please initialize the FPU before use."
#endif

int main(void)
{
	HAL_Init();

	__HAL_RCC_GPIOD_CLK_ENABLE();
	__HAL_RCC_GPIOD_CLK_ENABLE();

	GPIO_InitTypeDef led12_config = {
	    GPIO_PIN_12,
	    MODE_OUTPUT,
	    GPIO_NOPULL,
	    GPIO_SPEED_FREQ_HIGH,
	    ZERO
	  };
	  GPIO_InitTypeDef led13_config = {
	    GPIO_PIN_13,
	    MODE_OUTPUT,
	    GPIO_NOPULL,
	    GPIO_SPEED_FREQ_HIGH,
	    ZERO
	  };

	  HAL_GPIO_Init(GPIOD, &led12_config);
	  HAL_GPIO_Init(GPIOD, &led13_config);


	  /* Configure the system clock */
	  SystemClock_Config();

    /* Loop forever */
	for(;;){
		// turn on PB6
		    HAL_GPIO_WritePin(GPIOB, led1_config.Pin, GPIO_PIN_SET);

		    // wait for 0.5s
		    HAL_Delay(DELAY_TIME);

		    // turn off PB6
		    HAL_GPIO_WritePin(GPIOB, led1_config.Pin, GPIO_PIN_RESET);

		    // and turn on PC7
		    HAL_GPIO_WritePin(GPIOC, led2_config.Pin, GPIO_PIN_SET);

		    // then wait for 0.5s
		    HAL_Delay(DELAY_TIME);

		    // turn off PB6
		    HAL_GPIO_WritePin(GPIOC, led2_config.Pin, GPIO_PIN_RESET);
	}
}
